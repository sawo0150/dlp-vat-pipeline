# @package _global_

ingest:
  mode: "pipeline" # pipeline: generate task로 만든 manifest에 사진만 추가
  
  # 장비가 저장하는 소스 경로
  source_camera_dir: "C:/LabVIEW_Data/Session_current"
  file_ext: "*.bmp"
  
  # 정렬 방식 (보통 장비는 시간순/번호순으로 저장하므로 이름 정렬 후 1:1 매칭)
  match_strategy: "sort_order"